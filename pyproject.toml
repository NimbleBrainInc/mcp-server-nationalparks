[project]
name = "nationalparks-e2e"
version = "0.0.0"
description = "E2E tests for National Parks MCP server"
requires-python = ">=3.13"

[dependency-groups]
dev = [
    "mcp>=1.3.2",
    "pytest>=8.4.2",
    "pytest-asyncio>=0.24.0",
    "pytest-httpserver>=1.1.0",
    "requests>=2.32.0",
    "testcontainers>=4.9.0",
]
